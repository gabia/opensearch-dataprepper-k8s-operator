apiVersion: dataprepper.gabia.com/v1alpha1
kind: DataPrepperCluster
metadata:
  labels:
    app.kubernetes.io/name: dataprepper-operator
    app.kubernetes.io/managed-by: kustomize
  name: datapreppercluster-sample
spec:
  # Reference a DataPrepperClass for image and default resources, or set
  # spec.image directly. Spec values override class defaults when both are set.
  classRef: dataprepper-2-15
  replicas: 1
