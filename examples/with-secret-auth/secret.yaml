# In real environments create the Secret out-of-band (sealed-secrets,
# external-secrets, kubectl create secret generic, etc.) so credentials
# do not live in the manifest.
apiVersion: v1
kind: Secret
metadata:
  name: opensearch-credentials
  namespace: operator-lab
type: Opaque
stringData:
  OS_USER: admin
  OS_PASSWORD: REPLACE_ME
